# Release Notes 2026-08-12（v0.3.1）

[English](RELEASE_NOTES_20260812.md) | **繁體中文**

這是一輪「交付正確性」的更新。v0.3.0 交付了截圖處理管線；把一整批研究報告語料跑過之後，浮現出品質閘門看不見的一類缺陷——輸出格式完好、每項檢查都通過，但漏掉或弄壞的，剛好就是檢索查詢會落在的那些字。以下每一項修正都是對整份語料量測後才決定的，而不是只看最初出問題的那幾篇。（這一輪期間語料從 100 篇成長到 167 篇，所以底下的分母不一致。）

前一版：[Release Notes 2026-07-17（v0.3.0）](RELEASE_NOTES_20260717.md)，目前只有英文版。

## 主要更新

### 交付正文，而不是印正文的那張紙

- 版面裝飾（頁眉、頁碼、卷期行）本來有偵測函式卻沒有任何正式呼叫端：1,826 行被當成正文段落交付出去，散布在 1,843 個 chunk 中的 851 個裡。三個交付面現在都會略過它們；另外，在兩頁以上重複出現於相同邊界帶的短文字，也會一併標記為版面裝飾。
- 補字流程（重新讀取每一頁，補上沒有任何 block 涵蓋的片段）反而在重複與打亂它本來要補完的段落。涵蓋判斷改為有方向性、跨前後各一頁、忽略 markdown 跳脫與空白，並在任何比對都比不到時退回 bbox 包含判斷。補進來的片段現在是逐頁插進解析器的閱讀順序，而不是把整份文件依垂直位置重排——過去只要一個補字片段，就足以讓雙欄版面的左右欄交錯。
- 封面標題直排的文件，交付出來的 `H1` 是壞的——直排文字的 OCR 會掉字與換字——而同一個標題的乾淨版本，其實就以橫排頁眉的形式閒置在 IR 裡。167 篇中有 49 篇受影響。頁眉現在對「字」有權威性，但對「標題有幾段」沒有：封面有而頁眉沒有的前導詞會保留，頁眉多出來的系列標籤則不採用。

### 表格

- **多列表頭。** 「表頭只有一列」的假設，讓子欄位名稱掉進 fallback（`欄位4: 22.40`，沒有任何字說明它是進口占比），而子欄位那一列本身被當成第一筆資料交付。表頭列現在會在四道防護下合併，每一道都有實際的反例支撐。佔位欄名：926 → 8。
- **表格名稱。** 解析器沒有給 caption 時（128 張表中的 52 張），名稱來自一個用子字串比對挑出來的文件層級字串，再截斷到 100 字。現在改用印在表格正上方、距離最近的 caption；52 張中有 46 張取回了它實際印出的名稱。
- **數字表格被當成 OCR 雜訊丟掉。** 只要是純數字的儲存格就算「弱」，所以一張寬的統計表在結構上必然被判成亂碼，然後整表資料列被丟棄——4 篇報告中的 7 張表；這些數字消失後，又接連觸發空輸出檢查、VLM 稽核，以及一次約 10 萬 token 的 reviewer pass。格式良好的數字現在會被讀成資料；孤立單一數字與 `4.64.74.7` 這類黏在一起的字串仍算雜訊。11 張表全數復原。
- **出處列。** 印在表格框線內的 注／資料來源 那一行，長得跟「只有第一欄有值的資料列」一模一樣，於是被輸出成一筆「標題是一個句子、唯一欄位陳述了表格從沒說過的事」的記錄。現在改以「它自己的用語」辨識——比對範圍刻意收窄到論文實際會用的開頭——並排在所有記錄之後，成為結尾的一行。沒有自我宣告的列一律留作資料，因為欄位邊界被解析器弄丟的列同樣是滿版寬度，兩者在結構上無法區分。
- **欄位邊界崩塌的表格現在會被修復。** 這是新能力，詳見〈相容性注意事項〉。
- **`chunks.jsonl` 裡的表格標題。** 解析器把 caption 以字串陣列的形式回傳；76 個表格標題就這樣以 Python list 的字面樣貌交付出去，而 `chunks.jsonl` 正是檢索真正會讀的東西。caption 現在只在「組裝 payload 的那一處」攤平一次。同樣的形狀也讓圖片 caption 長度閘門對每一張「其實有 caption」的圖都誤觸發，導致模型一直在替旁邊早就印著說明文字的圖重新產生 caption。

### 繁體中文輸出

- 簡體內容偵測器的做法是用 `s2t` 轉換後與原文比對，但這組映射會把 台/布/占/干/了 —— 每一個都是標準繁體用字 —— 當成簡體。於是一般的台灣中文散文被判成簡體，接著被 `s2twp` 的詞表改寫（公布→公佈、關鍵零組件→關鍵零元件、數據→資料、機構名稱 台→臺）。現在會先摺疊有歧義的異體字對再做偵測，轉換改用 `s2tw`，之後再依位置還原作者自己的用字選擇。
- 轉換從來沒看過解析器自己產出的文字：它唯一的呼叫點在 VLM 的 render 路徑，因此 100 篇中有 88 篇、共 849 個明確的簡體字直接進了輸出。現在改為 normalize 的後處理，對每個帶文字的 payload 欄位執行，不管該 block 由誰產生；另加逐行的假名防護，避免把參考文獻裡的日文條目「修好」。
- 還有兩個偵測器根本看不到的字（凈、説——`s2t` 把它們映射到自己），因此位元組完全相同、從未進入轉換：分別出現 51 次與 25 次。

### VLM 穩健性

- **端點沒有提供的模型，現在會回報為不可用。** 可用性檢查只要 HTTP 端點有回應就回傳 true——包括剛剛才回報「設定的模型不在清單中」的那個分支——所以沒有 pull 過的模型、或模型名稱打錯，不會用一個人類看得懂的訊息把該階段一次擋下來，而是放行每一個補強請求、讓它們一個一個失敗。列不出任何模型的端點維持寬鬆（部分 gateway 會回傳空清單），且會比對 Ollama 隱含的 `:latest` tag，避免把可正常運作的裸名稱設定誤判為壞的。
- **小於視覺模型 32px patch factor 的裁切圖不再送出。** 四張 32x31、34x31、37x31px 的裁切圖造成 8 次 model runner panic 與 8 次補強遺失；伺服器回報給 client 的是一個看起來像記憶體不足、實際上不是的泛用資源錯誤。

### API、維運、資安

- **具名主機存取改為明確開啟。** 前端 dev server 以「允許所有 host」在 `0.0.0.0` 上執行，因此任何網站都可以 DNS rebinding 到它，並透過 `/api` proxy 打到沒有認證的後端。詳見下方 `SEMARK_FRONTEND_ALLOWED_HOSTS`；`localhost` 與直接用 IP 存取不受影響。
- **個資遮蔽現在在寫入邊界執行。** 過去分散在各個呼叫點，導致四個已交付的輸出面沒有被遮蔽：`dataset.md`、結構化修復後主文的重新匯出、`chunks.jsonl` 的 metadata，以及 `quality_gate.json` / `llm_vlm_outputs.md` 裡的 VLM 稽核節錄。
- **交付寫入改為原子操作。** 過去沒有任何交付產物是原子寫入的，而且分檔文件是「先刪掉舊的、再寫新的」，所以匯出中途崩潰會留下「已被刪除、卻仍被舊索引指向」的檔案。過期的分檔文件現在只會在新的一批落地之後才移除。
- **超過 500 個 run 之後的下載清單。** 在預設篩選條件下，outputs-summary 端點永遠只檢查最新的 500 個 run，並把這個視窗內的數量當成總數回報，所以規模超過之後，較舊的文件會從下載清單中無聲消失。列表查詢另外加上穩定的排序決勝鍵——`created_at` 只到秒，批次建立的資料列會同秒打平，導致跨頁時跳過或重複。
- **補強快取的身分認定現在會考慮解析設定。** 快取鍵既沒有納入解析設定、也沒有納入解析器版本，因此換設定重新解析後，可能拿到「屬於另一張圖」的 caption；切換輸出語言時則會拿到舊語言的 caption。此前已快取的資料會重新補強一次。
- **管線併發。** 現在由一個 module 層級的 semaphore 同時涵蓋佇列 worker 與直接呼叫的 `background=false` 路徑——後者過去會無上限地並行執行解析 + VLM 管線。啟動掃描也會取消因重啟而被孤立的 PENDING run（佇列在記憶體中），並且在資料庫斷線之前先停止 task queue，讓取消處理器仍能在關機過程中寫入 run 狀態。
- **Viewer。** bbox overlay 在每一頁都被垂直壓縮——A4 直式頁面上，頁面底部的 block 只畫到 71% 的高度，因此點選某個區域會選到錯的 block。package 階段刻意保留為原始 HTML 的表格，現在會透過 `rehype-raw` 搭配 `rehype-sanitize` 正常呈現在主文面板中，而不是被無聲丟棄（script、事件處理屬性與 inline style 仍會被移除）。相對路徑的圖片改由 asset endpoint 解析，不再打到 SPA 路由變成 404。
- **health 端點回報真實的應用程式版本**，不再是寫死的 `0.1.0`。

### 不是內容的內容

- 一個推廣插頁——含價格、訂購行與 QR code 的廣告——在 32 篇文件中占據最後一頁的右欄，而左欄仍然是參考文獻，因此這必須是「欄」層級的判斷而不是「頁」層級：丟掉整頁會把參考文獻一併刪掉。欄的邊界由該頁自己的版心間距推導，而不是寫死座標。這同時也讓 105 張書封與 QR code 不會進到模型。
- 一份二十頁報告的參考文獻，被拿去套用「為一頁式表單校準」的門檻來評分：`DOI:`、`https:` 開頭的條目，加上以冒號結尾的引用標題，都讀成了欄位標籤；而 申請單 也在 申請單位 之中被比對到。100 篇研究報告中有 5 篇被判定為可填寫表單，各自引發一個「結構化輸出為空」的問題與其下游稽核。
- 模型撰寫的圖片段落，過去以 `##` 標題輸出，與文章自己的章節同層級，而且沒有指名是哪一張圖。現在改為單一容器標題下的粗體子段落。移除 311 個多餘標題。

### 文件與 demo

- `examples/demos/README.md` 補上 v0.3.0 新增的兩個截圖 demo，以及它從未列出的 `raw-parse.md` / `figure-example.md` 檔案，並新增繁體中文版。
- 兩份 README 都補上表格崩欄修復的說明，另有三處「只存在於單一語言」的細節已同步。
- `BACKLOG.md` 記錄架構檢視發現、但這一輪刻意不動的已知限制。

## 相容性注意事項

- **新能力，預設開啟：** `vlm_repair_collapsed_tables`（`PipelineConfig` 欄位，沒有對應的環境變數）會重新讀取「欄位邊界被解析器併成單一儲存格」的表格——參考語料 128 張表中的 26 張——並從表格圖片還原格線。它與 `vlm_enrich_tables` 相互獨立，後者是為解析正常的表格加摘要。原始解析結果會以 `table_body_mineru` 一併保留；格線數量不齊、或轉錄字數不到印出字數 60% 的結果會被拒收，沿用原始解析。要維持舊行為請設為 `false`。
- **新變數：** `SEMARK_FRONTEND_ALLOWED_HOSTS`（逗號分隔）。透過具名主機存取前端，現在必須明確列出該主機名稱。用 `localhost` 或 IP 存取不受影響。
- **補強快取鍵已變更。** 此版之前快取的資料會重新補強一次。
- **已存 IR 中的 caption 形狀。** 此版之前解析的文件，其 `document_ir.json` 仍保有陣列形狀的 caption。讀取端的防護會涵蓋這些檔案，所以標題兩種情況都會正確輸出；但只有重新解析才會讓檔案裡實際存成攤平後的 caption。
- **health 端點的版本字串會改變**，從 `0.1.0` 變成真實的應用程式版本。
- `default.json` 中新增的 `template_section_labels`，在自備語料規則檔的部署中不會生效，因為語料規則是「取代」而非「合併」。
- 後端測試套件：437 → 718 個測試全綠。

## 驗證與但書

- 管線相關修正的驗證方式是：離線重放全部 167 份已儲存的 document IR，加上三份文件的端到端實跑。**尚未以重建後的 image 對整份語料做完整實跑。**
- 上文出現的所有門檻值——0.60 涵蓋率、40 字的表頭儲存格、60% 短讀下限、106 字的整表下限——都是對「這份語料」的量測結果，不是原則。表格註記方式不同的語料，必須重新量測。
- 補字涵蓋判斷的校準刻意是不對稱的：它寧可讓一句話重複出現，也不要讓一行只存在一份的文字消失。代價是兩個長度低於下限的整表重複內容會回來。
- 由於表格崩欄修復以圖片為準，模型讀錯的表格會被換成「錯的格線」而不是「糊掉的格線」。fail-closed 的防護就是這兩種結果之間唯一的界線。
- 仍然存在的已知限制：圖片的 footnote 目前仍不在補字涵蓋文字裡（同一類缺陷，尚未量測）；首字放大（drop cap）在解析器層級仍會掉第一個字；中文段落中夾帶日文引用時，該處的簡體字形會保留（全語料量測為 1 個字——以字元為單位切割有破壞日文的風險）。
